using UnityEngine;

/// <summary>
/// Holds the current weapon for a character that needs it.
/// </summary>
public class WeaponHolder : MonoBehaviour
{
    /// <summary>
    /// Saves the current weapon being held
    /// </summary>
    public Weapon CurrentWeapon;
}
